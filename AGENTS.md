# debug with local mcp

- User have setup the local mcp, just test it with `fs` mcp tool
- Ask user to rebuild, then continue the test
- Must use `cargo build`, do not use `cargo build --release` in dev
